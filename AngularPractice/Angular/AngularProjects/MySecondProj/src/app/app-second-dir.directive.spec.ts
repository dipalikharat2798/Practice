import { AppSecondDirDirective } from './app-second-dir.directive';

describe('AppSecondDirDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSecondDirDirective();
    expect(directive).toBeTruthy();
  });
});
