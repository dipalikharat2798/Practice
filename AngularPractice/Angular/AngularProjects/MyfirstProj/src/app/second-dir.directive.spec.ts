import { SecondDirDirective } from './second-dir.directive';

describe('SecondDirDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondDirDirective();
    expect(directive).toBeTruthy();
  });
});
