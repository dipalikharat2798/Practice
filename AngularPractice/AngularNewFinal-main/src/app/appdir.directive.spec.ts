import { AppdirDirective } from './appdir.directive';

describe('AppdirDirective', () => {
  it('should create an instance', () => {
    const directive = new AppdirDirective();
    expect(directive).toBeTruthy();
  });
});
